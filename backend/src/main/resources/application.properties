# Puerto en el que corre el servidor
# El frontend apunta a: http://localhost:8080/api/cuotas
server.port=8080

# Nombre de la aplicación (opcional, útil para logs)
spring.application.name=visacuotas-api
